// Deben inicializarse al mismo tiempo que se declaran
const productoNuevo = 'Tablet';

// Las variables declaradas const no se pueden reasignar
productoNuevo = 'Monitor';

console.log(productoNuevo);